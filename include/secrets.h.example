#pragma once

// WiFi credentials
static const char* WIFI_SSID = "your-wifi-ssid";
static const char* WIFI_PASS = "your-wifi-password";

// Fallback location (used on first boot before captive-portal setup)
// Decimal degrees, positive = N/E, negative = S/W
#define DEFAULT_LAT  0.0000f
#define DEFAULT_LON  0.0000f

// Captive-portal setup AP
#define AP_SSID "shravclock-setup"
#define AP_PASS "your-ap-password"
